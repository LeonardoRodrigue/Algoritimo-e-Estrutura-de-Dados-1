package Etapa2;

import java.util.Scanner;

public class C10EX06 {
    public static void main(String[] args) {
        double a, b, c, x0, y0, r, p, distancia;
/*
        Scanner sc = new Scanner(System.in);

        System.out.print("Digite o valor de A: ");
        a = sc.nextDouble();
        System.out.print("Digite o valor de B: ");
        b = sc.nextDouble();
        System.out.print("Digite o valor de C: ");
        c = sc.nextDouble();

        do {
            System.out.print("Digite o valor de Xo: ");
            x0 = sc.nextDouble();
            System.out.print("Digite o valor de Yo: ");
            y0 = sc.nextDouble();
            r = (a * x0) + (b * y0) + c;
            p = (x0, y0);
            distancia = ((a*x0) + (b*y0) + c) / Math.sqrt(Math.pow(a,2) + Math.pow(b, 2));
        } while (r != p);*/
    }
}
